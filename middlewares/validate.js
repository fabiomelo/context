'use strict';

var messages = require('../utils/messages');
var httpStatusCode = require('../utils/httpStatusCode');


module.exports = {
  updatePosition: function(request, response, next){
      next();
  }
};